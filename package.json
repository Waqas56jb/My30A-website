{
  "name": "my30a-ops",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n server,admin,client -c cyan,magenta,green \"npm run dev --prefix server\" \"npm run dev --prefix admin\" \"npm run dev --prefix client\"",
    "build:admin": "npm run build --prefix admin",
    "build:client": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
